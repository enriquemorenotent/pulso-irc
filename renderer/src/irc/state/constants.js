import { ROLE_ORDER } from '../roles.js';

const STATUS_TARGET = '*status';
const MAX_MESSAGES = 500;
const LIST_MAX_ITEMS = 2000;
const PREFIX_ORDER = [...ROLE_ORDER, ''];

export { STATUS_TARGET, MAX_MESSAGES, LIST_MAX_ITEMS, PREFIX_ORDER };
